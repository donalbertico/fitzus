/** @type {import('./$types').Actions} */
export const actions = {
    default: async (event) => {
      console.log('papito');

      // transporter.sendMail(options)
      return { sucess: true }
    }
};
